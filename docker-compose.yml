services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: zoniq
      POSTGRES_PASSWORD: zoniq
      POSTGRES_DB: zoniq
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
