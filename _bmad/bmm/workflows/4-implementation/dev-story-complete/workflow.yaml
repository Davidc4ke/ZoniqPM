name: dev-story-complete
description: 'Complete story implementation by updating the story file, sprint status, and archiving the dev session. Final step in atomized development workflow. Use when user says "complete this story" or "finish story" after verification passes'

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
user_skill_level: "{config_source}:user_skill_level"
document_output_language: "{config_source}:document_output_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/_bmad/bmm/workflows/4-implementation/dev-story-complete"
instructions: "{installed_path}/instructions.xml"

# Workflow inputs
story_key: "" # Derived from session.yaml path or explicit parameter
dev_session_root: ".dev-session"
session_file: "{dev_session_root}/{story_key}/session.yaml"
plan_file: "{dev_session_root}/{story_key}/plan.md"
context_handoff_file: "{dev_session_root}/{story_key}/context-handoff.md"
progress_files_pattern: "{dev_session_root}/{story_key}/progress-*.md"
verification_file: "{dev_session_root}/{story_key}/verification.md"
story_file: "{implementation_artifacts}/{story_key}.md"
sprint_status_file: "{implementation_artifacts}/sprint-status.yaml"
implementation_artifacts: "{config_source}:implementation_artifacts"

# Workflow outputs
archived_session_path: "{dev_session_root}/.archive/{story_key}"
completed_story_file: "{story_file}"

# Command line parameters
parameters:
  - name: story-key
    type: string
    required: false
    description: Story key to complete (e.g., "2-0-home-page-dashboard-layout"). If not provided, uses the most recent dev session.
